import * as React from "react";
import { Card, CardContent, CardHeader } from '@material-ui/core';

export default () => (
    <Card>
        <CardHeader title="Welcome to WaveSSoundS" />
        <CardContent>We're glad you're here</CardContent>
    </Card>
    
);

